# Minification is disabled (minifyEnabled false) — this file is a placeholder.
# If you ever enable R8, nothing special is required: the app has no
# reflection-based code paths except the JavaScript bridge methods, which
# are kept automatically via the @JavascriptInterface annotation.
